{
  "name": "dashboard-prpgi",
  "version": "1.0.0",
  "description": "Dashboard PRPGI",
  "scripts": {
    "start": "npx live-server --port=8080",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
